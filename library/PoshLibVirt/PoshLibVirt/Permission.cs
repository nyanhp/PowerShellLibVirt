namespace PoshLibVirt
{
    /// Enumeration of possible permissions
    public enum Permission
    {
        /// Read/Write
        rw,

        /// Read-only
        ro,
        /// Shared Read/Write - no idea when this is set
        sh
    }
}